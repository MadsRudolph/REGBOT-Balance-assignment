\section{Balance Controller (Task 2)}
\label{sec:balance}

\subsection{Plant Transfer Function}
With the inner wheel-speed loop of Task~\ref{sec:wheel-speed} closed, the plant seen by the balance controller is identified by linearising the Simulink model from the velocity reference \texttt{/vel\_ref} to the tilt-angle output (port~1 of the \texttt{/robot with balance} subsystem). The resulting transfer function $G_{tilt}(s)$ is seventh order and open-loop unstable, with one pole in the right-half plane near $+8.7\,\mathrm{rad/s}$ corresponding to the inverted-pendulum falling mode.

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.48\linewidth}
        \includegraphics[width=\linewidth]{images/regbot_Gtilt_bode.png}
        \caption{Bode plot of $G_{tilt}$.}
        \label{fig:gtilt_bode}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\linewidth}
        \includegraphics[width=\linewidth]{images/regbot_Gtilt_pzmap_zoom.png}
        \caption{Pole-zero map (zoom). The orange ring marks the single RHP pole.}
        \label{fig:gtilt_pz}
    \end{subfigure}
    \caption{Plant from velocity reference to tilt angle, $G_{tilt}(s)$.}
\end{figure}

\subsection{Controller Design}
% TODO — document PILead + post-integrator design.
% Parameters: $N_i$, $\alpha$, $\tau_d$, $\tau_i$, $K_P$, $\gamma_M$, $\omega_c$.
% Include the phase contribution from the post-integrator in calculations.

\subsection{Stability Analysis (Nyquist)}
% TODO — Nyquist plot showing one CCW encirclement of -1 (since P = 1).

\subsection{Bode Plot (Open-Loop)}
% TODO

\subsection{Simulation Results}
% TODO

\subsection{REGBOT Experiment}
% TODO — mission script and step response from physical REGBOT.

\subsection{Comments}
% TODO
